Fix a real correctness gap in this repository's shared agent contract validator: JSON non-finite numbers (NaN, positive infinity, negative infinity, including numeric overflow such as 1e309) must not be accepted as valid JSON data.

Both load_json and validate must reject non-finite floats with ContractValidationError, including nested arrays and object values that are otherwise unconstrained by a schema. Preserve support for finite numbers, arbitrary-size Python integers, and booleans under the appropriate schema; do not accidentally treat bool as number. Preserve existing schema behavior and useful error diagnostics. Add meaningful regression tests and complete the repository's relevant mandatory checks, reporting any environment blockers truthfully.

Work in this checkout. Implement and test the fix; do not only provide advice. Keep the change focused; do not modify unrelated runtime behavior or weaken existing tests. You may repair a blocking development-environment issue if needed, but report it separately. Do not install or upgrade AI providers or change the model. Do not spawn additional agents or call agentctl jobs: this is a single-primary development observation. Do not push or use external services. Leave your source edits for the outer integrator, without committing, and return a concise account of changes, commands run, and unresolved failures. Do not inspect parent directories or other experiment runs.
