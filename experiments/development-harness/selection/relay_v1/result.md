# 保存候補の受入点検：事前助言の追加で分類は改善しなかった

2026-09-07、[事前固定protocol](protocol.md)の1組を実行した。
単独・事前助言ありとも、候補2件×範囲2群の分類が保存判定と一致し、採用を保留した。
相談側は時間・input・outputが増えた。この範囲で常時事前相談を追加する根拠は得られなかった。

| 観測 | solo | consult |
| --- | ---: | ---: |
| 分類一致 | 4/4 | 4/4 |
| 不一致 / unknown | 0 / 0 | 0 / 0 |
| 採用 | 保留 | 保留 |
| 条件時間（起動・検証・回収込み） | 132.980秒 | 195.559秒 |
| input tokens | 109,866 | 151,799 |
| cached input tokens | 0 | 0 |
| output tokens | 3,181 | 4,406 |
| model呼出し | maker 3 | advisor 1 + maker 3 |
| 公開probe | 2 | 2 |

相談側は時間47.06%、input38.17%、output38.51%増。
共通準備1.526秒、全体330.064秒。全7呼出しのusageが得られ、全containerと一時認証copyの回収を確認した。
各条件600秒・output12000、advisor120秒・output3000を超えていない。
上限は終了時観測であり、厳密なin-turn token制限とは呼ばない。

## 評価の範囲と採否

候補1は採用条件・パス境界とも要修正、候補2は採用条件は適合・パス境界は要修正、という保存ラベルと一致した。
4分類は同一課題内の項目で、4件の独立サンプルではない。両候補に既知の境界欠陥があり、
`null`だけで選択能力が分かる課題ではない。自由文の根拠は構造だけを確認し、意味の真偽はunknown。
旧候補の全品質を現在環境で再採点した結果でもない。

実装の新規生成、途中で相談の必要性を選ぶ方式、独立した検証担当、複数実装の比較は今回測っていない。
1組・固定順・既知課題・同じ締切のワークフロー比較であり、独立contextだけの効果を分離しない。
別課題やモデルへの一般化、品質付き速度の一般ランキング、金額への換算はしない。

事前の採否基準に従い、成果上積みは未確認とする。同課題で勝つまで追加runを行わず、
固定事前相談を既定に追加しない。単独にも公開検証を使わせる対照と、全呼出しの費用を数える方式は維持する。
今回の実装で「モデルの検証要求→隔離実行→結果受取り→最終提出→自動照合」が成立した。
この経路の完成と、協働による性能改善の成立を区別する。

## 出典と検証

[機械可読の集計・各呼出し・原記録hash](result.json)。実装commitは`46507dd`。
事前登録・生記録は `/home/asakura/.local/state/devcontainer-evaluations/lifecycle-relay-live-20260907-01/`。
原response・抽出content・print-only probe・採点stdoutのhashを結び、提出を修復・再生成せず照合した。
protocolの1組は使用済み。過去のrunner・oracle・得点は上書きしていない。

[sourceを固定した校正](validation.json)は実Docker7テスト・skipなし。
実CLIの疑似HTTP要求でtoolsなし、疑似providerの両条件でprobe受渡し・原文一致を確認した。
独立reviewが最終回収記録の欠落を指摘し、回収失敗・記録欠落・回収後評価例外の回帰確認を追加した。
修正後の再reviewで指摘解消を確認してから、liveを開始した。

## 現行の開発へ戻した確認

結果への独立批判では、次に別の固定事前相談を作るより、特定した未達が現行CLIに残るかを確認し、
残るなら修正する方が目的に近いと指摘された。現行実装は実行bitだけを比較し、root・二重slashの重なりも拒否する。
既存の採用・不一致拒否・境界の[3テスト](current-production-check.json)が4.434秒、skipなしで通った。
古い候補の欠陥は現行で解消済みなので、同じ修正を再実装せずこの題材を閉じる。
これは現行CLIの確認であり、live候補の再採点や協働による改修成果には数えない。
