# 動的課題の単独開発: 正常提出、元評価は時間上限で保留

2026-09-08。gpt-6-astra/high、Codex CLI 0.153.0で、固定した1開始を実施した。
**単独開発は正常に提出まで完了した。元の30秒/scenario評価は途中で上限に達したため、全域品質の判定は保留。**
その後の90秒枠による別診断は、元の測定を成功へ読み替えるものではない。

## 開発と元の評価

| 項目 | 結果 |
| --- | --- |
| 開発・実験・選択・最終応答 | 2380.145秒（39分40秒）、2400秒枠内 |
| reported input | 3,530,450 tokens |
| うちcached input | 3,389,184 tokens。inputの内数 |
| output | 52,233 tokens、80,000枠内 |
| 提出物 | 13,393 bytesの単一Python方策 |
| 公開固定check | 24/24有効 |
| 元の非公開評価 | 21測定、1時間超過で未測定、残る2例は未実行 |
| 元の固定参照評価 | 提出物の評価で停止したため未開始 |
| 元profile全体 | 2562.553秒、`withhold` |

単独はgreedy、予約、先の実行計画、複数計画の選択を実装・比較した。
追加の12開発ケース・12確認ケースや境界検査も作った。これらは開発側の自己検証であり、
独立評価の代わりにはしない。最終提出は、既に到着した仕事について3種類の計画を作り、
締切・価値・共有memory・cache・実行時間の観測を使って直近の割当を選ぶ方策だった。
別agent、追加LLM、非公開評価の助言は使っていない。

元評価が停止したのは`qualification-severe-cache-1`。551応答までにrequest時間が約21.9秒蓄積し、
起動・評価側処理等を含むscenario全体が30秒に達した。単独の思考限界や不正な方策とは判定しない。

親runnerは中断した採点結果を詳細へ取り込めず、回収をunknownのまま残した。
[別の回収監査](cleanup-audit.json)で、actor 1個と評価用22個の所有containerが残っていないこと、
認証一時copyが削除されていることを確認した。元記録のflagは上書きしていない。
集計器はこのpartial結果も読み、21/1/2と未開始の参照を保持するよう修正した。

## 別の時間上限診断

[固定した診断契約](../timing_diagnostic_v1/TASK.md)に基づき、モデルを再実行せず、
同じ提出原文と固定参照を同じ24例で採点した。総scenario上限だけ90秒にし、
1応答5秒・1 CPU・256 MiB・物理時計・品質規則は維持した。

以下は各帯の8例を分子・分母でまとめた診断値。全12系列/帯と24例の原値を
[診断の機械可読結果](../timing_diagnostic_v1/result.json)へ保持する。

| 負荷帯 | 単独の期限内価値 | 固定参照の期限内価値 | 単独の最重要完了率 | 固定参照の最重要完了率 |
| --- | ---: | ---: | ---: | ---: |
| ordinary | 92.4% | 88.6% | 62/65（95.4%） | 60/65（92.3%） |
| contended | 80.3% | 67.2% | 146/163（89.6%） | 120/163（73.6%） |
| severe | 72.5% | 58.2% | 357/403（88.6%） | 291/403（72.2%） |

12系列/帯の集計では期限内価値・最重要完了数とも参照以上だった。
個別には`qualification-contended-burst-1`で価値が10、`qualification-ordinary-scarce-1`で4だけ参照を下回った。
最重要クラスの完了数は、今回の24例では参照を下回らなかった。
これは非agent参照との診断比較であり、協働の効果・最適性・安定した成功率の実証ではない。

提出物は最長scenario約40.6秒、最大1応答約0.39秒で全24例を完了した。
元評価で測定できた21例は、得点・完了時刻・traceがすべて一致した。
このことは総時間上限の余裕不足という診断を支持する。wall timeには変動があり、純CPU時間とは呼ばない。
評価全体は提出物389.229秒、参照185.809秒。品質の改善と計画計算の費用を両方残す。
診断側48containerの回収も実状態で確認した。

## 判断と次の作業

この単独は、既存の配置探索参照よりかなり強い方策を作れた。参照水準を満点にする評価へは戻さない。
一方、重い負荷の最重要クラスは46件が未達で、他のclassや計算費用との両立にも探索余地がある。
46件が全てonlineで救えるとは証明していない。全仕事を処理できない過負荷条件とも区別する。

今回は「強い単独の品質プロファイル」と「評価器の総時間上限の不足」を得た。
次は同じ動的課題を維持し、総時間と1応答の時計を事前固定・検証した新protocolで、
強い単独と適応的に協働する開発を比較する。元の保留runを正式な比較の単独対照には流用しない。
qualificationは既知の探索/診断データになったため、確認用と再命名しない。
最重要サービスを保ちながら価値を増やすこと、候補選択・統合で品質を失わないことを比較の問いへつなげる。
協働側の全参加者usage・停止・公開能力を事前に確認し、追加の参照実装や題材変更を先に進めない。

## 証拠

実装commit `b86135d`。原提出SHA-256:
`a40833985e7d034a9fdf704a0e1b04a35812bad3a943f99654275f5a01bd05e1`。
[元profile要約](result.json)、[事前検証](validation.json)、[元protocol](protocol.md)。
生記録: `/home/asakura/.local/state/devcontainer-evaluations/dynamic-solo-live-20260908-01/`。
外側の計画・実装・reviewのモデルusageは未測定で、上のdeveloper usageに含めない。
追加のlive開始・候補の事後差替え・confirmation評価は行っていない。
