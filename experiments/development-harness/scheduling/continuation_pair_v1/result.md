# 継続開発pair: 単独の期限停止により比較を保留

2026-09-09。**単独が40分の開発期限で停止し、固定protocolに従って協働の開始と独立採点を止めた。**
1開始を使用し、未使用の協働枠もこのpairでは閉じる。追加開始・再試行・候補交換はしない。
この実行から協働の到達力や改良効果は測定できていない。

## 何が回収できたか

| 項目 | 記録 |
| --- | --- |
| bridgeの停止理由 | `TimeoutError: development deadline` |
| 開発時計 | 2400.059秒。終了検出・停止処理を含み、CPU時間ではない |
| CLIターン | 完了記録なし。unfinished participant / incomplete or aborted turn |
| 原quality / output予算適合 | withhold / unknown |
| 原cohort | withhold、barrierなし、独立採点0ケース |
| 提出物 | 24,838 bytesをcontainer削除後に封印 |
| 既知usage | 81応答、input 5,644,117、うちcached 5,514,368、output 55,844以上 |
| 回収 | solo container・認証コピーを削除済み |

提出SHA256は`6ff0ab8400b3b0320d99b7ed4b08dc0ff1e8b77c4d2d7c2824e2e51885e1b6e6`。
developerの`EXPERIMENTS.md`に記された最終sourceのhashとも一致する。
使用量はpartial。未記録分をゼロにせず、全output上限適合・請求完全性・費用効率を認定しない。
前回の初期source作成費と外側の準備・review費も、この追加開発のusageとは別である。

開始前にはsource一致の実Docker 5試験と公開校正を完了していた。
実行後も49 sourceのcheckout・保存snapshotとの一致と、提出hash・回収を確認した。
規定どおりの期限停止であり、0.059秒だけを丸めて正常終了とする根拠はない。
最後の説明文だけが原因だったと断定できる記録もなく、CLIのターン未完了として扱う。

## 公開側の開発記録

提出物の公開checkは全24ケースvalid、returncode 0で保存されている。
初期sourceは期限内価値11161・最重要534、提出物は11325・542だった。
未完了価値は1525→1481。これは**developerの公開記録**であり、今回止めた独立評価の代わりではない。

単独は予約・dispatch・whole-chain admission・処理時間推定・資源に応じた切替などを試した。
より高い公開得点の候補も、生成例での重要class退行を理由に棄却した。
残り時間を推定処理時間が越えるだけで実行を見送る問題に、短い反例を作り、予測予約の扱いを変えた。
生成例ではclass 4の期限内完了や打切り付き期限超過量の退行も残している。

初期sourceの公開checkは、CPU負荷の高い別実験と同時に実行した際に最後の1ケースで時間上限に達した。
developerはそのケースを単独で再検証し、全24例の初期値を照合した。
source変更が混入した別の比較も採用判断から明示的に除外した。
これらの開発中の再検証は40分内の活動で、追加のmodel開始ではない。

## 測定契約の見直し

成果物と公開検証を保存していても、ターンが終わらないと現在の契約では全品質を保留する。
これは「正常終了した開発episodeを比較する」という規則には合うが、
「予算が尽きた時点でどこまで良い成果があるか」という主目的とは別の条件を測定へ加えている。

今回の原判定・時計・usage・未開始枠は変更しない。
次は、通常終了と予定した期限停止を区別したまま、**停止・回収後に固定できた指定成果物**を
評価できる別契約を疑似providerで検証する。停止要求時刻と実際に回収できた時間の差も残す。
不正な提出は品質の失敗として区別し、未提出・回収不明・source不一致は評価可能としない。
現runの採点再開や、協働側だけを後から付け足すための変更にはしない。

原記録: `/home/asakura/.local/state/devcontainer-evaluations/scheduling-continuation-pair-20260909-01/`。
全文・原記録hashと判定軸は[result.json](result.json)。
