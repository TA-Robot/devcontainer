# Continue from an existing implementation

Read `/public/initial.py`. This is the common starting implementation for this
development episode. Copy it into `/work` to develop and test it. The public
contract, not the initial implementation, defines correctness and the objectives.
The initial implementation may contain weaknesses. You may modify it, combine it
with alternatives, replace it entirely, or retain it if your experiments support
that decision. Save the single selected implementation as `/work/submission.py`.

Use the public checker and your own experiments to understand remaining failures
and test improvements. Both conditions receive the same starting bytes and public
tools. Previous developers' reports, private evaluation inputs and scores are not
provided. Include the starting implementation in your public comparisons. Keep
important-class regressions and unfinished work visible when selecting a result.
There is no requirement to produce a minimum number of edits or new candidates.
All reading, development, experiments, selection and final response are inside
the 2400-second development budget already stated in your instructions.
