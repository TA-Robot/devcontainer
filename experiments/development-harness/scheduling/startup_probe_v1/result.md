# 2026-09-10: 固定6観測の結果

2試験、skipなし、26.006秒で通過した。6観測とも事前予想と一致し、全containerを回収した。
元transport・同じ固定image・1 CPU・256 MiB、応答5秒・scenario総時間90秒で実行した。
ready条件だけは固定fixtureのmarkerを待つ。待機上限20秒は総時間90秒の内側に置く。

| 固定fixture | 旧時計 | marker待機後の時計 |
| --- | --- | --- |
| 通常 | 応答成功、0.688秒 | 待機0.466秒、応答成功0.001秒 |
| marker前に6秒待つ | 初回応答5.001秒で停止、応答0 | 待機6.366秒、応答成功0.003秒 |
| 要求受信後に6秒待つ | 応答5.001秒で停止 | 待機0.327秒、応答5.001秒で停止 |

旧時計に起動待ちが混入する構造は再現した。以前の初回タイムアウトがDocker、Python、
ホストI/Oのどこで発生したかは、この固定fixtureからは断定できない。
単発の成功を安定した速度の証拠にも使わない。

採用する修正は、評価器所有のlauncherが準備完了を通知し、要求側が応答時計を開始してから
候補の実行を許す双方向の受渡し。通知後すぐ候補を起動すると、通知の配送待ちに候補が先行実行
できるため、候補の実行許可まで必要である。候補の読込み・import・初期化は最初の5秒に含める。

原記録は`~/.local/state/devcontainer-evaluations/startup-probe-20260910-01/`。
`plan.json`のsource hash、固定policy、各trialの記録、`result.json`を保持する。
実モデル開始0、旧得点の変更0。この結果だけではlive比較の開始条件を満たさない。
