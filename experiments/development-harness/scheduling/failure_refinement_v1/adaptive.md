Develop the strongest online scheduling policy you can. Read /public/TASK.md and
/public/RUNTIME.md. Your workspace is /work. Compare substantially different
implementations, generate cases and tests, challenge your assumptions and select
using broad public evidence. Prioritize on-time value and critical-class service;
do not stop at FIFO or interface conformance. Save /work/submission.py.

Native collaboration is explicitly authorized. Use it to improve the available
solutions, including when the existing candidates share a weakness. Consultation,
independent approaches, competing prototypes, counterexample search, review and
implementation delegation are available; choose based on the current obstacle.

Ground collaboration in an unresolved scheduling failure or a testable difference
between approaches. A failing trace, a plausible missed opportunity, or conflicting
results can motivate a task. Separate unavoidable overload or unavailable future
information from a decision that a legal online policy might improve. Do not treat
the gap to perfect delivery as evidence that a proposed improvement is feasible.
Early independent exploration is also useful when competing assumptions are not
yet tested; do not require a mature incumbent before seeking another approach.

When asking for an independent alternative, share the task and relevant observable
evidence, and let its author propose a mechanism before seeing your preferred
implementation when that would reduce anchoring. Independence is a temporary
information boundary, not a ban on later discussion. Derive participants from
distinct uncertainties and useful artifacts; algorithm names and role labels alone
do not establish diversity.

Use disagreements to produce a distinguishing test, a changed implementation, or
evidence that the disagreement does not affect the result. After comparing real
candidates, check whether their important failures overlap. If selection among
them leaves the same weakness, consider revising a shared assumption or combining
mechanisms into a new tested implementation. Another ranking of unchanged
candidates cannot repair a failure common to that set. Investigate only while the
expected benefit justifies the remaining implementation and validation time.

Use concise existing experiment reports and source revisions to carry evidence;
do not build a reporting framework or count discussions as progress. Narrow tests
help explain a mechanism; validate any adopted change broadly, including critical
regressions, before selection. Unmodified baselines must remain available. Treat
an integration as a new candidate and test its actual source. Preserve rejected
candidates and brief reasons. You own the final selection and integration.

Use separate candidate files to avoid simultaneous writes. All participants must
inherit gpt-6-astra/high; do not override model or effort. Use only native
collaboration tools for model calls, not additional CLI sessions or direct APIs.
The native runtime offers four concurrent slots including you; this is a capacity
limit, not a recommended team size. All experiments share the published CPU and
memory limits. Do not assume concurrent benchmark processes add compute capacity.

You have 2400 seconds of shared wall time, including all participants, experiments,
coordination, selection and the final response. The observed output-token ceiling
is 160000 summed across all participants and compaction calls, not per agent.
Give child tasks bounded deadlines and finish all children before your final
response. You may finish early. End with a concise account of tested approaches,
collaboration choices, adopted/rejected evidence and remaining weaknesses.
No hidden feedback is provided.
