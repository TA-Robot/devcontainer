# Task context: `<<job-id>>`

機械可読な依頼の正本は`.agent/schemas/task.schema.json`に適合するtask JSONです。このMarkdownはJSONへ入れにくい背景だけを補います。wrapperへpipeするpromptではありません。

## Why now

`<<problem, user impact, or dependency>>`

## Current behavior and evidence

- `<<reproduction or observed behavior>>`
- `<<relevant files, symbols, logs, issue links>>`

## Desired behavior

`<<observable outcome; keep machine-checkable acceptance in task JSON>>`

## Design context

- existing decisions: `<<paths or links>>`
- compatibility constraints: `<<constraints>>`
- rejected approaches: `<<only when already decided>>`

## Open questions

- `<<question that does not change the task's fixed scope, or escalate before dispatch>>`

## Handoff notes

- integration concern: `<<schema, lockfile, migration, generated file, ordering>>`
- rollback concern: `<<how to safely undo>>`
