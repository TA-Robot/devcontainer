#!/usr/bin/env bash
# Codex バックエンド用の薄いシム。実体は second-agent（共通エンジン）。
# 環境変数 CODEX_SA_* と既存のコマンド名・挙動を維持する。
set -euo pipefail
here="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export SA_BACKEND=codex
export SA_PROG=codex-second-agent
exec "${here}/second-agent" "$@"
