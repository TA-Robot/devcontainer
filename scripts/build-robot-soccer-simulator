#!/usr/bin/env bash
set -euo pipefail

repo_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
image=${ROBOT_SOCCER_SIMULATOR_IMAGE:-robot-soccer-simulator:dev}

exec docker build \
  --file "$repo_root/experiments/robot-soccer-control/Dockerfile" \
  --tag "$image" \
  "$repo_root"
