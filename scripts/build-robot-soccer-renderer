#!/usr/bin/env bash
set -euo pipefail

repo_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
image=${ROBOT_SOCCER_RENDERER_IMAGE:-robot-soccer-renderer:dev}

exec docker build \
  --file "$repo_root/experiments/robot-soccer-control/Dockerfile.renderer" \
  --tag "$image" \
  "$repo_root"
