# プロジェクトの進め方への独立批判

2026-09-07。ユーザーの明示依頼でgpt-6-astra / reasoning effort lowの3体へ相談した。
対象sourceは`c3eda2e`。これはproject方針へのadvisoryで、協働効果のbenchmarkではない。
以下はprimaryによる意見の要約と採否。agentの多数一致を効果の証拠にはしない。

## 相談の範囲

- 判断対象: ユーザーの目的に対して、現在の実験と次の作業が適切か。
- 期待する作用: 目的・評価・実利用という異なる観点からのcoverageと、primaryの判断への独立批判。
- participant: `critique_strategy`、`critique_experiments`、`critique_product`。
- 共通知識: 目的、主要文書、直近の結果、primaryの直前提案。互いの回答は共有せず初回意見を得た。
  同じmodelと共通説明を使っており、primaryの説明によるanchoringまで排除したとは言えない。
- 作業境界: native advisory。read-onlyは依頼上の制約で、強制隔離ではない。
  編集・実験・追加agentは依頼せず、primaryも3者の回答が揃うまで対象文書を変更しなかった。
- 人数3は今回のユーザー指定。各観点を返した時点で終了し、一般的な推奨人数やround数にはしない。
- primaryの責任: 根拠の確認、異論の扱い、採否、文書への反映。usageと人間のreview費はここでは未測定。

## 3者の意見

| 観点 | 批判 | 次に勧めたもの |
| --- | --- | --- |
| 目的・優先順位 | 「独立確認候補」と「次に確認すべき候補」を混同している。目的と方式の対応表は既にあり、総論の再設計も過剰になり得る | Cycle 002/003を基に、仕様追加と統合を伴うepisodeで単体継続と分担・統合を比較できるか監査する |
| 評価・比較 | queue確認が成功しても変えられる判断が狭い。既存review比較も、maker自身のcheckpointへの自己review対独立reviewとは情報履歴が違う | template lifecycleで、単体の逐次試作・選択と、独立した複数実装・選択・統合を比較する。最終成果に加え、良い候補を選べなかった失敗も観測する |
| 実利用・配布 | 実験promptへの知識還元と、配布先agentが通常の入口から知識を使える状態が離れている | 既存skill/playbookへ範囲付きの実測を接続し、新しい要求系列で同じ協働能力の案内改良前後を比較する |

共通して、外部動作評価・停止・全参加者の消費回収・原結果保持は残すべき前進と判断した。
queueの負の結果も有用であり、小課題を無価値として捨てる意見ではない。
一方、queueの速度差確認を主作業にする根拠は弱く、全方式対応の新engineや計画総論の再構築も勧めなかった。

## 根拠をprimaryが確認した箇所

- [プロジェクト計画](../project-plan.md)と[協働実験計画](collaboration-experiment-plan.md)には、
  方式・対照・成果・改善先が既にある。primaryの「対応を組み直す」は、実行順位の変更へ縮小する。
- [conditionalの結果](../../experiments/development-harness/queue_review/conditional_v1/result.md)は
  両側ともsubmit・4/4。情報介入の候補判定は保持できるが、相談起動判断の改善は観測していない。
  時間差が再現してもこの制約は残る。
- [queue v1 protocol](../../experiments/development-harness/queue_review/v1/protocol.md)は
  初期コードのsolo点検・修正とreviewer→fresh makerの比較である。
  自分が実装したmakerの見落としに対する独立reviewの効果へ一般化しない。
- [Cycle 002](development-harness-cycle-002.md)では35/35でもpermission等の要求違反が残り、
  [Cycle 003](development-harness-cycle-003.md)の要求別評価がその不足を識別している。
  継続要求・互換性・復旧の評価資産は既存であり、大規模benchをゼロから作る必要性はまだ示されていない。
- repository自身の`AGENTS.md`は[実測ガイド](collaboration-evidence.md)を参照する。
  一方、[配布テンプレ](../../AGENTS_TEMPLATE.md)から辿る
  [orchestration skill](../../project/.codex/skills/orchestrate-agent-collaboration/SKILL.md)と
  [playbook](../../project/docs/agents/collaboration-playbook.md)はproject-localな観測を読むが、
  確認した入口には今回のqueue等の比較結果への導線がない。
  配布先での利用を確認した実行証拠も今回の調査では得ていない。

## 採否と次の作業

**採用する判断:** queueの順序反転確認を当面の主作業から外す。
登録済みの「独立確認候補」は変更せず、過去の結果・protocolも保持する。
案内を近く既定化する具体的需要が生じた場合は、確認の優先度を改めて評価する。

**primaryの提案も修正する:** 目的や方式表を再び大きく書き直さない。
既存資産から次の未解決判断を一つ選び、評価と実利用への接続を進める。

次の作業は、Cycle 002/003のtemplate導入・更新・復旧を対象にした適格性監査。
成果物は、既存source・公開要求・評価の再利用箇所と不足を示す比較設計一件とする。
監査で確かめる項目は以下。

1. 仕様追加後の互換性・既存編集保持・復旧・統合を外部動作で判定でき、妥当な別実装を受理できるか。
2. 独立した複数実装と公開checkによる選択を、既存runnerの狭い追加で比較できるか。
   これを第一候補とする理由は、方式比較が未着手で、候補生成・選択失敗・最終成果を分けて観測できるため。
   分担・統合は、要求から独立作業境界を示せる場合の代替候補。両方式を一介入に束ねない。
3. 強いsoloにも自己検証・別案・逐次試作を許し、同時間での成果と全消費を区別できるか。
4. 結果を通常の配布入口へ渡す最小の変更箇所を特定し、成果が正・負・不明の各場合に案内の何を変えるか。

第一候補はplanning priorで、scopeは次の比較の選定、ownerはprimary/integrator。
評価の識別力不足、実要求との不一致、接続費過大なら採用を撤回し、その理由を残す。
今回の相談では方式・要求系列・予算のlive protocolは固定していない。

3者の提案には、既知課題で方式を測る案と、新しい要求系列で配布案内を測る案の違いがある。
既知課題は校正や探索に再利用できるが、未使用課題と呼ばない。
案内の改善効果を確認する段階では、方式探索で結果を見た要求から分離した系列を使う。
新しい要求の根拠は実際の開発上の必要性とし、soloが失敗するまで難化しない。

次のmilestoneは、未測定の一方式について、最終成果と全費用から範囲付きの採否を一件増やし、
通常のproject入口でagentが参照できる状態へ戻すこと。
協働の勝利・発動数は完了条件にしない。改良案が出た場合の別系列での再評価も計画へ繋ぐ。

## この相談で変わったことと限界

次の作業順位を変更し、primaryが提案した総論の再設計を取り下げた。
配布先への導線と、既存review比較の情報履歴という別の不足が明確になった。
これは相談から得た判断材料であり、solo対照がないため、この相談の品質・速度の改善量は判定しない。
次の方式の最終選定、評価の適格性、配布先での実利用、単体を超える成果は未確認。
